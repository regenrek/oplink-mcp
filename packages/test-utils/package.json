{
  "name": "@oplink/test-utils",
  "version": "0.0.1",
  "private": true,
  "description": "Shared test utilities for Oplink SDK and CLI",
  "type": "module",
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "import": "./dist/index.mjs"
    }
  },
  "scripts": {
    "build": "pnpm exec rolldown -c",
    "prepublishOnly": "pnpm run build",
    "test": "pnpm run build && vitest",
    "test:1": "pnpm run build &&vitest run 'CL1'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "catalog:prod"
  },
  "devDependencies": {
    "@types/node": "catalog:types",
    "rolldown": "1.0.0-beta.49",
    "typescript": "catalog:cli"
  }
}
