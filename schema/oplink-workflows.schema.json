{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://oplink.dev/schema/oplink-workflows.schema.json",
  "title": "Oplink Workflows",
  "type": "object",
  "description": "Map of workflow names to definitions",
  "definitions": {
    "parameter": {
      "type": "object",
      "properties": {
        "type": { "type": "string", "enum": ["string", "number", "boolean", "array", "object", "enum"] },
        "description": { "type": "string" },
        "required": { "type": "boolean" },
        "enum": { "type": "array", "items": { "type": ["string", "number", "boolean"] } },
        "items": { "$ref": "#/definitions/parameter" },
        "properties": {
          "type": "object",
          "additionalProperties": { "$ref": "#/definitions/parameter" }
        },
        "default": { "type": ["string", "number", "boolean", "array", "object", "null"] }
      },
      "required": ["type"],
      "additionalProperties": false
    }
  },
  "additionalProperties": {
    "type": "object",
    "properties": {
      "description": { "type": "string" },
      "prompt": { "type": "string" },
      "runtime": { "type": "string", "enum": ["prompt", "scripted", "external"] },
      "externalServers": {
        "type": "array",
        "items": { "type": "string", "minLength": 1 }
      },
      "parameters": {
        "type": "object",
        "additionalProperties": {
          "type": "object",
          "properties": {
            "type": { "type": "string", "enum": ["string", "number", "boolean", "array", "object", "enum"] },
            "description": { "type": "string" },
            "required": { "type": "boolean" },
            "enum": { "type": "array", "items": { "type": ["string", "number", "boolean"] } },
            "items": { "$ref": "#/definitions/parameter" },
            "properties": {
              "type": "object",
              "additionalProperties": { "$ref": "#/definitions/parameter" }
            },
            "default": { "type": ["string", "number", "boolean", "array", "object", "null"] }
          },
          "required": ["type"],
          "additionalProperties": false
        }
      },
      "steps": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "call": { "type": "string", "minLength": 1 },
            "args": { "type": "object" },
            "save_as": { "type": "string", "minLength": 1 },
            "requires": { "type": "string", "minLength": 1 },
            "quiet": { "type": "boolean" }
          },
          "required": ["call"],
          "additionalProperties": true
        }
      },
      "tools": {
        "description": "Optional shorthand for situational/sequential tool lists",
        "oneOf": [
          { "type": "string" },
          {
            "type": "object",
            "additionalProperties": { "type": ["string", "object"] }
          }
        ]
      },
      "toolMode": { "type": "string", "enum": ["sequential", "situational"] },
      "context": { "type": "string" }
    },
    "allOf": [
      {
        "if": { "properties": { "runtime": { "const": "scripted" } } },
        "then": { "required": ["steps" ] }
      },
      {
        "if": { "properties": { "externalServers": { "minItems": 1 } } },
        "then": { "required": ["prompt"] }
      }
    ],
    "additionalProperties": true
  }
}

/*
 Note: this schema defines a recursive reference to parameter definitions via
 `#/definitions/parameter`. Define the shared parameter shape here.
*/
